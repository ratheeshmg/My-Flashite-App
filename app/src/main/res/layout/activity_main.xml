<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#E0E0E0"
    tools:context=".MainActivity">

    <ImageButton
        android:id="@+id/flashButton"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerInParent="true"
        android:src="@android:drawable/ic_lock_power_off" android:tint="#FFFFFF"
        android:background="@drawable/rounded_button_bg" android:scaleType="fitCenter"
        android:contentDescription="Flashlight Toggle Button" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/flashButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:text="Tap to turn ON"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#333333"/>

</RelativeLayout>
